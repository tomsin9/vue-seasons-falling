name: Deploy Demo to GitHub Pages

on:
  push:
    branches:
      - main  # 只有 push 到 main 分支時才觸發

permissions:
  contents: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v4

      - name: Setup Node ⚙️
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install and Build 🔧
        run: |
          npm install
          npm run build:demo

      - name: Deploy 🚀
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: dist-demo # should be the same as the outDir in vite.config.js
          branch: gh-pages  # deploy to this branch